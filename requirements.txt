# FleetForge OSS — Python Dependencies

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
geoalchemy2==0.15.2
psycopg2-binary==2.9.10

# Auth & Security
python-jose[cryptography]==3.3.0
# bcrypt used directly — passlib 1.7.4 is unmaintained and breaks with bcrypt >= 4.x
bcrypt==4.2.1
pydantic[email]==2.10.0
pydantic-settings==2.6.1

# Data Processing & Optimization
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
pulp==2.9.0
# ortools==9.11.4210  # Google OR-Tools (optional, heavy)

# Async Task Queue
celery==5.4.0
redis==5.2.0
kombu==5.4.2

# HTTP Client
httpx==0.28.0

# Monitoring
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0
# tests default to an in-memory SQLite engine (TEST_DATABASE_URL overrides it)
aiosqlite==0.20.0